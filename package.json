{
  "name": "nextai-finance-app-monorepo",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/schema"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prisma": "^5.22.0"
  },
  "dependencies": {
    "form-data": "^4.0.4"
  }
}
